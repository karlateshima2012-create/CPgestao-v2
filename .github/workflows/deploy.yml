name: Find Project Root

on:
  push:
    branches:
      - master

jobs:
  find_root:
    runs-on: ubuntu-latest
    steps:
      - name: Search for Artisan
        uses: appleboy/ssh-action@master
        with:
          host: 46.202.186.144
          username: u176367625
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: 65002
          script: |
            echo "--- BUSCANDO ARTISAN ---"
            find . -name "artisan" -maxdepth 6
            
            echo "--- ESTRUTURA domains/creativeprintjp.com ---"
            ls -F domains/creativeprintjp.com/ 2>/dev/null || echo "not found"
            
            echo "--- ESTRUTURA domains/creativeprintjp.com/public_html ---"
            ls -F domains/creativeprintjp.com/public_html/ 2>/dev/null || echo "not found"
