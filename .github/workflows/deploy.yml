name: Inspect Project Structure

on:
  push:
    branches:
      - master

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: List Project Files
        uses: appleboy/ssh-action@master
        with:
          host: 46.202.186.144
          username: u176367625
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: 65002
          script: |
            cd domains/creativeprintjp.com/public_html/cpgestao
            echo "--- ESTRUTURA CPGESTAO ---"
            ls -F
            echo "--- ESTRUTURA api_backend ---"
            ls -F api_backend/ 2>/dev/null || echo "api_backend not found"
            echo "--- ESTRUTURA frontend ---"
            ls -F frontend/ 2>/dev/null || echo "frontend not found"
