name: Debug Server Paths

on:
  push:
    branches:
      - master

jobs:
  debug_paths:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and List Folders
        uses: appleboy/ssh-action@master
        with:
          host: 46.202.186.144
          username: u176367625
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: 65002
          script: |
            echo "--- PWD ---"
            pwd
            echo "--- LS RAÍZ ---"
            ls -F
            echo "--- LS domains/ ---"
            ls -F domains/ 2>/dev/null || echo "domains/ não encontrado"
            echo "--- LS public_html/ ---"
            ls -F public_html/ 2>/dev/null || echo "public_html/ não encontrado"
            echo "--- BUSCANDO BACKEND ---"
            find . -name "backend" -type d -maxdepth 5
